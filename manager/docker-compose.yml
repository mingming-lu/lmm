version: "3.7"
services:
  manager:
    image: node:10.15.1-alpine
    volumes:
      - .:/manager
    environment:
      TZ: Asia/Tokyo
    networks:
      - lmm

networks:
  lmm:
    external:
      name: lmm
