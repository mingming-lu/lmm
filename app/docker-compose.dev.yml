version: "3.7"
services:
  app:
    command: npm --prefix /app run dev
    environment:
      SERVER_HOST: '0.0.0.0'
      SERVER_PORT: 8001
      API_URL: http://host.docker.internal:8002
      API_URL_BROWSER: https://api.lmm.local
      ASSET_URL: https://asset.lmm.local

# ref: https://docs.docker.com/docker-for-mac/networking/#per-container-ip-addressing-is-not-possible
