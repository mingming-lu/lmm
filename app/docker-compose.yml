version: "3.7"
services:
  app:
    image: node:10.16.0-alpine
    volumes:
      - .:/app
    environment:
      TZ: Asia/Tokyo
    networks:
      - lmm

networks:
  lmm:
    external:
      name: lmm
